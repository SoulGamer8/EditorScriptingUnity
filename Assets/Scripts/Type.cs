using System.Collections;
using System.Collections.Generic;
using UnityEngine;

namespace Type
{
    public enum MageDmgType{
        Ice,
        Fire
    }

    public enum MageWeaponType{
        Staff,
        Book
    }

    public enum WarriorClassType{
        Defender,
        Berserker
    }

    public enum WarriorWeaponType{
        OneHandedSword,
        TwoHandedSword,
        DualWieldedSwords
    }

    public enum RogueWeaponType{
        Daggers,
        Bow

    }

    public enum RogueStategyType{
        Stealth,
        Speed

    }
}
